export const models_path = (models) => {
  return `/${models}`;
};

export const models_create_path = (models) => {
  return `/${models}/create`;
};


export const model_path = (model, id) => {
  return `/${model}/${id}`;
};